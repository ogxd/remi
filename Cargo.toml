[package]
name = "remi"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/ogxd/remi"

[dependencies]
clap = { version = "4", features = ["derive"] }
chrono = "0.4"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
